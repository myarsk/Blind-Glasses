# ── Face recognition ──────────────────────────────────────────────────────────
# InsightFace uses ONNX — no dlib, works on Python 3.13 + ARM
# If pip install insightface fails, first run: pip install Cython
insightface
onnxruntime
opencv-python-headless

# ── Camera ────────────────────────────────────────────────────────────────────
picamera2

# ── GPIO / switches ───────────────────────────────────────────────────────────
RPi.GPIO

# ── Distance sensor (VL53L0X via I2C) ────────────────────────────────────────
# VL53L0X (John Bryan) works with clone chips; adafruit's library rejects them
smbus2
VL53L0X

# ── GPS ───────────────────────────────────────────────────────────────────────
pynmea2
pyserial

# ── Voice output ──────────────────────────────────────────────────────────────
# Requires system package: sudo apt install espeak-ng
pyttsx3

# ── Telegram ──────────────────────────────────────────────────────────────────
python-telegram-bot>=21.0

# ── Vision agent ──────────────────────────────────────────────────────────────
openai
# Requires system package: sudo apt install portaudio19-dev
SpeechRecognition
pyaudio

# ── Shared ────────────────────────────────────────────────────────────────────
numpy
