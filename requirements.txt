# ── Face recognition ──────────────────────────────────────────────────────────
# InsightFace uses ONNX — no dlib, works on Python 3.13 + ARM
# If pip install insightface fails, first run: pip install Cython
insightface
onnxruntime
opencv-python-headless

# ── Camera ────────────────────────────────────────────────────────────────────
picamera2

# ── GPIO / switches ───────────────────────────────────────────────────────────
RPi.GPIO

# ── Distance sensor (VL53L0X via I2C) ────────────────────────────────────────
# adafruit lib is used but with model-ID patch for clone chip compatibility
adafruit-blinka
adafruit-circuitpython-vl53l0x

# ── GPS ───────────────────────────────────────────────────────────────────────
pynmea2
pyserial

# ── Voice output ──────────────────────────────────────────────────────────────
# Uses espeak-ng directly via subprocess — no Python package needed
# System package required: sudo apt install espeak-ng

# ── Telegram ──────────────────────────────────────────────────────────────────
python-telegram-bot>=21.0

# ── Vision agent ──────────────────────────────────────────────────────────────
openai
# Requires system package: sudo apt install portaudio19-dev
SpeechRecognition
pyaudio

# ── Shared ────────────────────────────────────────────────────────────────────
numpy
